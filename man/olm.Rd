% Generated by tools/generate-man.R -- do not edit by hand
\name{olm}
\alias{olm}
\title{OLM}
\description{
Run an ordinary-least-squares model with enhanced output.
}
\usage{
olm(..., newey.west = 0, stdcoefs = TRUE, include.anova = TRUE, keep.pval = FALSE, truncate.T = TRUE, digits = 4)
}
\arguments{
  \item{...}{Arguments passed to lm().}
  \item{newey.west}{Lag for Newey-West standard errors. Default 0.}
  \item{stdcoefs}{Include standardized coefficients. Default TRUE.}
  \item{include.anova}{Include ANOVA sum of squares decomposition. Default TRUE.}
  \item{keep.pval}{Keep p-values in output. Default FALSE.}
  \item{truncate.T}{Truncate t-statistics to +/-99.99. Default TRUE.}
  \item{digits}{Rounding digits. Default 4. }
}
\value{
An object of class "olm" inheriting from "summary.lm". 
}
\seealso{
print.olm

Other regression: \code{\link{famamacbeth}}, \code{\link{neweywest}}, \code{\link{ols.nona}}, \code{\link{ooslm}}, \code{\link{print.olm}}, \code{\link{residuals}}
}
\examples{
\dontrun{
# Basic regression
df <- data.frame(y = c(1, 2, 3, 4, 5), x = c(1.1, 1.9, 3.2, 3.8, 5.1))
fit <- iaw$olm(y ~ x, data = df)
print(fit)

# With NA values (na.exclude keeps original indices in residuals)
df2 <- data.frame(y = c(1, NA, 3, 4, 5), x = c(1, 2, 3, 4, 5))
fit2 <- iaw$olm(y ~ x, data = df2)
print(fit2)

# Multiple predictors; disable standardised coefficients
df3 <- data.frame(y = rnorm(20), x1 = rnorm(20), x2 = rnorm(20))
fit3 <- iaw$olm(y ~ x1 + x2, data = df3, stdcoefs = FALSE)
print(fit3)

}
}
