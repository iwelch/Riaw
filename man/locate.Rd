% Generated by tools/generate-man.R -- do not edit by hand
\name{locate}
\alias{locate}
\title{Locate a File}
\description{
Search for file locally, one directory up, one down, then system-wide.
}
\usage{
locate(global.filename, restrict.pattern = NULL, strict = TRUE, omit.backup = TRUE)
}
\arguments{
  \item{global.filename}{Filename to find.}
  \item{restrict.pattern}{Optional regex the path must match.}
  \item{strict}{If TRUE, error when multiple matches. Default TRUE.}
  \item{omit.backup}{Exclude /backup/ and /old/ paths. Default TRUE. }
}
\value{
Full pathname, or NULL if not found. 
}
\seealso{

Other io: \code{\link{lock}}, \code{\link{read.csv}}, \code{\link{Riolog}}, \code{\link{touch}}, \code{\link{write.csv}}
}
\examples{
\dontrun{
# Find a data file anywhere on disk (searches locally first, then system-wide)
iaw$locate("mydata.csv")

# Restrict matches to a particular project directory
iaw$locate("config.R", restrict.pattern = "/myproject/")

# Allow multiple matches (returns most recently modified)
iaw$locate("README.md", strict = FALSE)

# Find a configuration file scoped to a project
iaw$locate("settings.json", restrict.pattern = "/analytics/")

# Include backup directories in search results
iaw$locate("archive.csv", omit.backup = FALSE)
}

}
