% Generated by tools/generate-man.R -- do not edit by hand
\name{ols.nona}
\alias{ols.nona}
\title{Fast OLS Without NA Checking (Deprecated)}
\description{
Deprecated. Use \code{RcppArmadillo::fastLm} instead.
}
\usage{
ols.nona(...)
}
\arguments{
  \item{...}{Ignored. Function always stops with deprecation message. }
}
\value{
Never returns; always stops with an error. 
}
\seealso{

Other regression: \code{\link{famamacbeth}}, \code{\link{neweywest}}, \code{\link{olm}}, \code{\link{ooslm}}, \code{\link{print.olm}}, \code{\link{residuals}}
}
\examples{
\dontrun{
# ols.nona is deprecated; calling it always stops with a message
iaw$ols.nona()
# Use RcppArmadillo::fastLm instead:
# library(RcppArmadillo)
# fastLm(y ~ x, data = df)

# Attempting any call raises a deprecation error
tryCatch(iaw$ols.nona(y ~ x), error = function(e) e$message)
# "please use library(RcppArmadillo); ..."

# Recommended replacement for fast OLS without NA overhead
# library(RcppArmadillo)
# X <- cbind(1, rnorm(100))
# y <- rnorm(100)
# fastLmPure(X, y)$coefficients  # numeric vector of beta-hats
}

}
