% Generated by tools/generate-man.R -- do not edit by hand
\name{read.csv}
\alias{read.csv}
\alias{searchdir}
\alias{read.csv.gz}
\alias{read.fst}
\alias{fread}
\title{Fast CSV File Reading}
\description{
Reads CSV files using data.table::fread for speed.
}
\usage{
searchdir(filename, ..., select = NULL, search = NULL, allow.fst.cache = TRUE, quiet = FALSE)
}
\arguments{
  \item{filename}{Path to CSV, CSV.GZ, or FST file.}
  \item{...}{Arguments passed to \code{data.table::fread}.}
  \item{select}{Character vector of column names to read, or \code{NULL} for all (default).}
  \item{search}{Directory to add to search path.}
  \item{allow.fst.cache}{Logical. Use FST cache if available (default \code{TRUE}).}
  \item{quiet}{Logical. If \code{TRUE}, suppress file info message (default \code{FALSE}). }
}
\value{
Data frame. 
}
\seealso{

Other io: \code{\link{Riolog}}, \code{\link{lock}}, \code{\link{touch}}, \code{\link{write.csv}}
}
\examples{
\dontrun{
# Basic CSV read
df <- iaw$read.csv("data.csv")

# Read only selected columns (fread-style column selection)
df <- iaw$read.csv("data.csv", select = c("date", "price", "volume"))

# Read compressed file, suppress progress message
df <- iaw$read.csv("data.csv.gz", quiet = TRUE)

# Add a search directory so relative filenames resolve automatically
df <- iaw$read.csv("prices.csv", search = "~/data/")
}
}
