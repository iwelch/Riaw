% Generated by tools/generate-man.R -- do not edit by hand
\name{sst}
\alias{sst}
\title{Total Sum of Squares}
\description{
Calculates total sum of squares.
}
\usage{
sst(x)
}
\arguments{
  \item{x}{Numeric vector. }
}
\value{
SST value. 
}
\seealso{

Other statistics: \code{\link{autocorrel}}, \code{\link{autopcorrel}}, \code{\link{cmpsum}}, \code{\link{colN}}, \code{\link{colSds}}, \code{\link{covp}}, \code{\link{dependent.rank}}, \code{\link{print.cor}}, \code{\link{rank}}, \code{\link{rowSds}}, \code{\link{sdp}}, \code{\link{sse}}, \code{\link{summary}}, \code{\link{tabularsummary}}, \code{\link{varp}}
}
\examples{
\dontrun{
iaw$sst(c(1, 2, 3, 4, 5))   # 10  (sum of squared deviations from mean 3)

# Constant vector: SST is 0
iaw$sst(rep(7, 5))           # 0

# SST = (n-1) * var(x)
x <- c(2, 5, 1, 8, 3)
iaw$sst(x)
(length(x) - 1) * var(x)    # same value

# Use SST to compute R-squared manually: 1 - SSR/SST
y <- c(1.2, 2.1, 3.0, 4.2, 5.1)
fit <- lm(y ~ c(1:5))
1 - sum(resid(fit)^2) / iaw$sst(y)   # equals summary(fit)$r.squared

# Two-element vector: SST = (x1-mean)^2 + (x2-mean)^2
iaw$sst(c(10, 20))   # 50

# SST of daily stock returns measures total variability
set.seed(1)
returns <- rnorm(252, mean = 0.0004, sd = 0.01)
iaw$sst(returns)   # total variation to be explained by a model

# NA values are handled gracefully
iaw$sst(c(1, 2, NA, 4, 5))   # computed on non-NA values only
}
}
