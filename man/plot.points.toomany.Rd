% Generated by tools/generate-man.R -- do not edit by hand
\name{plot.points.toomany}
\alias{plot.points.toomany}
\title{Density Heatmap for Large Scatterplots}
\description{
Bins (x, y) into a grid and draws density-colored rectangles.
Useful when a scatterplot has too many points to render individually.
}
\usage{
\method{plot}{points.toomany}(x, y, bandwidth, inlogs=TRUE, colstrength=1, ...)
}
\arguments{
  \item{x}{X values.}
  \item{y}{Y values.}
  \item{bandwidth}{Numeric vector of length 2: c(x_binwidth, y_binwidth).}
  \item{inlogs}{If TRUE (default), color by log(1 + count) for better contrast.}
  \item{colstrength}{Multiplier for color intensity. Default 1.}
  \item{...}{Additional arguments passed to rect(). }
}
\value{
Invisible NULL. 
}
\seealso{

Other plotting: \code{\link{as.PlotDate.yyyymmdd}}, \code{\link{plot.boxtext}}, \code{\link{colorblind}}, \code{\link{plot.contour}}, \code{\link{plot.ecdf.lineplot}}, \code{\link{plot.hist2d}}, \code{\link{hline}}, \code{\link{iaw.boxplot}}, \code{\link{plot.labelcurve}}, \code{\link{lambda2color}}, \code{\link{plot.loess.lineplot}}, \code{\link{MixColor}}, \code{\link{plot.native.slope}}, \code{\link{plot.p.arrows}}, \code{\link{plot.pdf.embedfonts}}, \code{\link{pdf.end}}, \code{\link{plot.pdf.start.bera}}, \code{\link{pdf.start}}, \code{\link{plot.timearea}}, \code{\link{plot.worldmap}}, \code{\link{plot.shade.xyy}}, \code{\link{plot.text.bg}}, \code{\link{plot.text.onbg}}, \code{\link{vline}}, \code{\link{plot.yearly.tickmarks}}, \code{\link{plot.ylab.left}}
}
\examples{
\dontrun{
# Density heatmap for 10,000 points that would be overplotted as dots
set.seed(1)
x <- rnorm(10000)
y <- rnorm(10000)
plot(range(x), range(y), type = "n", xlab = "x", ylab = "y",
     main = "Density heatmap")
iaw$plot.points.toomany(x, y, bandwidth = c(0.1, 0.1))

# Correlated data with coarser bins
y2 <- 0.7 * x + rnorm(10000, sd = 0.7)
plot(range(x), range(y2), type = "n")
iaw$plot.points.toomany(x, y2, bandwidth = c(0.2, 0.2), inlogs = FALSE)

# Daily stock return scatter (e.g., SPY vs QQQ) with fine bins
set.seed(99)
spy <- rnorm(5000, 0, 0.01)
qqq <- 1.1 * spy + rnorm(5000, 0, 0.005)
plot(range(spy), range(qqq), type = "n",
     xlab = "SPY return", ylab = "QQQ return", main = "Return heatmap")
iaw$plot.points.toomany(spy, qqq, bandwidth = c(0.002, 0.002))
abline(lm(qqq ~ spy), col = "red", lwd = 2)

# Boost color intensity with colstrength for sparse data
set.seed(5)
x3 <- rnorm(500)
y3 <- rnorm(500)
plot(range(x3), range(y3), type = "n", main = "Boosted color")
iaw$plot.points.toomany(x3, y3, bandwidth = c(0.3, 0.3), colstrength = 3)
}

}
