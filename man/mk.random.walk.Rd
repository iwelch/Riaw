% Generated by tools/generate-man.R -- do not edit by hand
\name{mk.random.walk}
\alias{mk.random.walk}
\title{Generate Random Walk}
\description{
Creates random walk series.
}
\usage{
mk.random.walk(n, start = 0, sd = 1)
}
\arguments{
  \item{n}{Length of series.}
  \item{start}{Starting value.}
  \item{sd}{Standard deviation of steps. }
}
\value{
Numeric vector. 
}
\seealso{

Other utilities: \code{\link{ascii}}, \code{\link{base}}, \code{\link{beep}}, \code{\link{bisection}}, \code{\link{cat.both}}, \code{\link{cat.stderr}}, \code{\link{chatter}}, \code{\link{check.names}}, \code{\link{debug.advice}}, \code{\link{debug.on}}, \code{\link{dict.lookup}}, \code{\link{done}}, \code{\link{enable.error.line.num}}, \code{\link{exit}}, \code{\link{grepcolname}}, \code{\link{heartbeat}}, \code{\link{index.of.variable}}, \code{\link{instrumentR}}, \code{\link{kable}}, \code{\link{latex}}, \code{\link{latlon.distance}}, \code{\link{latlonx}}, \code{\link{mklatlonid}}, \code{\link{invlatlon}}, \code{\link{testlatlon}}, \code{\link{ls.objects}}, \code{\link{meminfo}}, \code{\link{mkRdictionary}}, \code{\link{msg}}, \code{\link{nearest}}, \code{\link{now}}, \code{\link{numobs.by}}, \code{\link{object.size.MB}}, \code{\link{osinfo}}, \code{\link{rdraw.perfect}}, \code{\link{require.variables}}, \code{\link{rexp.perfect}}, \code{\link{rnorm.perfect}}, \code{\link{runall}}, \code{\link{setupknitr}}, \code{\link{sink}}, \code{\link{sprint.data.frame}}, \code{\link{strcat}}, \code{\link{whatis}}, \code{\link{which.nearest}}
}
\examples{
\dontrun{
# Default: 100-step walk starting at 0 with SD = 1
set.seed(42)
rw <- iaw$mk.random.walk(100)
length(rw)   # 100

# Start from a non-zero level
iaw$mk.random.walk(10, start = 100)

# Volatile walk: larger step size
set.seed(1)
iaw$mk.random.walk(10, sd = 5)

# Simulate two independent price paths
set.seed(7)
p1 <- iaw$mk.random.walk(50, start = 100, sd = 1)
p2 <- iaw$mk.random.walk(50, start = 100, sd = 2)
cor(p1, p2)   # near zero for independent walks

# Walk of length 1 is just the starting value
iaw$mk.random.walk(1, start = 42)  # 42

# Simulate a stylized stock price over 252 trading days
set.seed(2024)
daily_price <- iaw$mk.random.walk(252, start = 100, sd = 1.5)
range(daily_price)  # approximate price range over one year

# Multiple walks for a Monte Carlo fan chart
set.seed(99)
paths <- replicate(50, iaw$mk.random.walk(100, start = 0, sd = 0.5))
dim(paths)  # 100 x 50 matrix of simulated paths
}
}
