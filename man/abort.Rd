% Generated by tools/generate-man.R -- do not edit by hand
\name{abort}
\alias{abort}
\title{Abort Program Execution with Error Message}
\description{
Terminates program execution with an informative error message.
}
\usage{
abort(errstring)
}
\arguments{
  \item{errstring}{A character string describing the error. }
}
\value{
Does not return; stops execution. 
}
\seealso{

Other error-handling: \code{\link{assert}}
}
\examples{
# Basic abort with a plain message
\dontrun{
iaw$abort("File not found")
}

# Abort with a dynamic message built from context
\dontrun{
load_data <- function(path) {
  if (!file.exists(path))
    iaw$abort(paste("Cannot find file:", path))
  readRDS(path)
}
load_data("/no/such/file.rds")
}

# Combined with \%or\% for readable guard clauses
\dontrun{
(is.numeric(x)) \%or\% iaw$abort("x must be numeric")
}
}
